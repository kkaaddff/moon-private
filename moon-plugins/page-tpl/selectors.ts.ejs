import {IAllReducerProps} from "./types";
import {ReducerKey} from "./constant";

/*
 WARNING: 在复杂页面使用统一store2Props,会有性能 问题, 详情,请查看;
*/
export function store2Props(allStore: any):IAllReducerProps {
  return {
    <% pageModel.dataModels.forEach(actor=>{ %>
        <%=actor.instanceName%> : allStore[ReducerKey.<%=actor.instanceName%>],
    <% }) %>
  }
}

