import _ from 'lodash';
import {immerUtil} from "moon-runtime";
import { <%=Util.getReducerTsName(actor.fileName)%> } from "../types";

export const INITIAL_STATE: <%=Util.getReducerTsName(actor.fileName)%>  = {
  isReady:false,
  <% actor.datas.forEach(dataItem=>{ %>
             <%=dataItem.name%>:<%- JSON.stringify(dataItem.value) || Util.getDefaultByType(dataItem.type)%>,
  <% }) %>
};


 <% actor.methods.forEach(event=>{ %>

/*
<%=event.comment||""%>
*/
export function <%=event.name%>(state:<%=Util.getReducerTsName(actor.fileName)%>,payload:any) :<%=Util.getReducerTsName(actor.fileName)%>{
    <%=event.content%>
  return state;
}
 <% }) %>

// create by moon https://github.com/creasy2010/moon
