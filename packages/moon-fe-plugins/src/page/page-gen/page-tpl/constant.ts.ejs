export const pageKey="<%=pageModel.className%>";
export const BASE = pageKey+'_';

export const ReducerKey ={
<% pageModel.dataModels.forEach(actor=>{ %>
<%=actor.instanceName%>: pageKey+"<%=actor.className%>",
<% }) %>
}


export const  Command ={
//通用修改数据方法
commonChange : BASE+'commonChange',
init : BASE+'init',
clean : BASE+'clean',

<% pageModel.dataModels.forEach(actor=>{ %>
<%=actor.instanceName%>: {
<% actor.methods.forEach(event=>{ %>
<%=event.name%> :  BASE+"<%=actor.name%>_<%=event.name%>",
<% }) %>
},
<% }) %>
}
