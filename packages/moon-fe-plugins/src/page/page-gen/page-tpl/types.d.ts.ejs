<% pageModel.dataModels.forEach(actor=>{ %>
        export interface I<%=actor.className%>Reducer {
            isReady:boolean;
            isLoading?:boolean;
            <% actor.datas.forEach(dataItem=>{ %>
                         <%=dataItem.name%>:<%=dataItem.typeName||"any"%>;
            <% }) %>
        }
<% }) %>

export type IAllReducerProps  = {
    <% pageModel.dataModels.forEach(actor=>{ %>
                <%=actor.instanceName%>  : I<%=actor.className%>Reducer ,
    <% }) %>
  [name:string]:any;
}

export type I<%=pageModel.className%>Props={ };
export type I<%=pageModel.className%>State={ };

 <% pageModel.subComponents.forEach(item=>{ %>
export type I<%=item.className%>Props = { };
export type I<%=item.className%>State = { };
 <% }) %>

// create by moon https://github.com/creasy2010/moon
