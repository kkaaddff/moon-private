import React, {useState, PropsWithChildren} from 'react';
import * as T from '../types.d';
import pageModel from '../actions';
import {  useSelector } from 'react-redux';
import {store2Props} from "../selectors";
import classNames from "classnames";
import styles from './<%=subComp.name%>.less';
<%-subComp.imports||""%>

export default (props: PropsWithChildren<T.I<%=subComp.className%>Props>) => {
        const [ state,  setState ] = useState<T.I<%=subComp.className%>State>({});
        const {
        <% pageModel.dataModels.forEach(actor=>{ %>
        <%=actor.instanceName%>,
        <% }) %>
        }: T.IAllReducerProps = useSelector(store2Props)

        <%-tplContent.initMethodContent%>
        <%-tplContent.otherMethodsContent%>

        return <div className={styles.<%-pageModel.className+subComp.className%>}>
            <%-tplContent.renderContent%>
        </div>;
};
