export const pageKey="<%=Util.toUCamelize(pageInfo.pageKey)%>";
export const ReducerKey ={
<% pageInfo.models.forEach(actor=>{ %>
        <%=Util.toLCamelize(actor.fileName)%>: pageKey+"<%=Util.toUCamelize(actor.fileName)%>",
<% }) %>
}

export const BASE = pageKey+'_';

export const  Command ={
  //通用修改数据方法
commonChange : BASE+'commonChange',
init : BASE+'init',
clean : BASE+'clean',

 <% pageInfo.models.forEach(actor=>{ %>
     <%=Util.toLCamelize(actor.fileName)%>: {
      <% actor.methods.forEach(event=>{ %>
        <%=event.name%> :  BASE+"<%=event.moduleName%>_<%=event.name%>",
      <% }) %>
     },
 <% }) %>
}


// create by moon https://github.com/creasy2010/moon
